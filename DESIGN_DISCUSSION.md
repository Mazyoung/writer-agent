# 小说写作 Agent — 设计方案

## 项目目标

构建一个面向长篇网文的**多 Agent 自动写作框架**，支持人工在任意节点介入，大多数情况下全自动运行。

- **目标类型**: 长篇网络小说（分章节创作）
- **LLM**: DeepSeek V4 系列（OpenAI 兼容 API）
  - `deepseek-v4-flash`: 性价比模型，支持 thinking/non-thinking 双模式（取代旧 V3/R1）
  - `deepseek-v4-pro`: 高端模型，支持 thinking/non-thinking 双模式，性能最强
- **核心理念**: 通过分模块、分章节降低单次任务复杂度，通过状态管理保证长文本一致性

---

## 一、整体架构

```
                        ┌──────────────────┐
                        │   人机交互层      │
                        │  文件级介入       │
                        │  _edited.md 优先  │
                        └────────┬─────────┘
                                 │
                        ┌────────▼─────────┐
                        │   编排器          │
                        │   Orchestrator   │
                        │   章节级调度      │
                        └────────┬─────────┘
                                 │
        ┌────────────────┬───────┴────────┬────────────────┐
        │                │                │                │
   ┌────▼────┐     ┌────▼────┐     ┌────▼────┐     ┌─────▼─────┐
   │ 架构者   │     │  作者   │     │ 监督者  │     │状态管理师 │
   │Architect│     │ Author  │     │Supervisor│    │State Mgr  │
   └─────────┘     └─────────┘     └─────────┘     └───────────┘
        │                │                │                │
   ┌────┴────┐     ┌────┴────┐     ┌────┴────┐     ┌────┴────┐
   │1A 世界观│     │2A 章节  │     │3A 一致性│     │4A 状态  │
   │  构建师 │     │  规划师 │     │  守护者 │     │  更新师 │
   │(Pro-thk)│     │(Flash-t)│     │(Flash)  │     │(Flash)  │
   ├─────────┤     ├─────────┤     ├─────────┤     ├─────────┤
   │1B 情节  │     │2B 场景  │     │3B 质量  │     │4B 简报  │
   │  设计师 │     │  写手   │     │  审阅者 │     │  生成师 │
   │(Pro-thk)│     │ (Pro)   │     │(Flash-t)│     │(Flash-t)│
   └─────────┘     ├─────────┤     └─────────┘     └─────────┘
                   │2C 章节  │
                   │  润色师 │
                   │ (Pro)   │
                   └─────────┘

        ┌──────────────────────────────────────┐
        │              存储层                   │
        │  SQLite(状态) + Markdown(文档)        │
        │  + ChromaDB(向量检索) + JSON(配置)    │
        └──────────────────────────────────────┘
```

---

## 二、九大 Agent 详细设计

### 模块一：架构者 (Architect)

职责：创建和管理故事中相对稳定的内容——背景设定、角色、大纲。

#### 1A — 世界观构建师 (World Builder)
- **产出**: 世界规则文档（分"铁律"和"设定"两层）
- **铁律**（不可变）: 魔法/修炼体系的基本规则、世界物理法则
- **设定**（半可变）: 地理、势力、历史、文化
- **模型**: `deepseek-v4-pro` (thinking 模式)，复杂世界观设计需要最强推理能力
- **调用频率**: 全书初始化时1次，重大设定变更时手动触发

#### 1B — 情节设计师 (Plot Designer)
- **产出**: 三层大纲体系
  - **卷大纲**（Volume）: 大方向，全书分几个大阶段
  - **弧大纲**（Arc，10-30章）: 完整故事段落，有起承转合
  - **章大纲**（Chapter）: 单章概要，200-500字
- **变更权限**: 章大纲可自动微调，弧大纲需人工确认，卷大纲变更需人工审批
- **附加产出**: 角色初始档案（外貌、性格、动机、能力、关系网）
- **模型**: `deepseek-v4-pro` (thinking 模式)，复杂情节结构设计需要深度推理
- **调用频率**: 全书初始化 + 每卷/每弧开始时

---

### 模块二：作者 (Author)

职责：把设定和大纲转化为小说正文。

#### 2A — 章节规划师 (Chapter Planner)
- **输入**: 章大纲 + 角色当前状态 + 上一章结尾 + 监督者约束 + 简报
- **核心决策**: 判断当前章节是"延续型"还是"新展开型"，然后决定检索策略
  - **延续型**: 重点读前3章完整文本 + 出场角色状态 + 全局铁律精简版
  - **新展开型**: 重点读角色状态表完整版 + 相关世界观设定 + 伏笔登记表未回收项
- **产出**: 场景级写作大纲（3-8个场景），每个场景含：发生什么、涉及角色、情绪曲线、字数预估
- **模型**: `deepseek-v4-flash` (thinking 模式)，温度 0.5，需要结构化推理但成本敏感
- **调用频率**: 每章1次

#### 2B — 场景写手 (Scene Writer)
- **输入**: 当前场景大纲 + 相关角色档案 + 上一场景结尾 + 风格指南
- **工作机制**: 逐个场景写作，每场景 500-1500 字
- **产出**: 场景正文
- **模型**: `deepseek-v4-pro` (non-thinking 模式)，温度 0.8-1.0。**输出质量最重要，这是读者直接看到的内容，使用最强模型**
- **调用频率**: 每章 3-8 次（最高频 Agent）

#### 2C — 章节润色师 (Chapter Polisher)
- **输入**: 所有场景文本 + 章节大纲
- **任务**: 拼接场景、统一语气、修复过渡、确保字数达标
- **产出**: 完整章节文本 + 章节元数据（新增角色、推进的伏笔、关键事件摘要）
- **模型**: `deepseek-v4-pro` (non-thinking 模式)，温度 0.6。**面向读者的最终文本，质量优先**
- **调用频率**: 每章1次

---

### 模块三：监督者 (Supervisor)

职责：保证创作内容前后统一、情节合理、符合架构设计。

#### 3A — 一致性守护者 (Consistency Guard)
- **写前介入**: 审核场景级大纲，检查是否和角色设定/世界观冲突
- **写中介入**: 每个场景写完后做快速抽查
  - 角色行为是否符合性格？
  - 对话是否 OOC？
  - 能力设定是否冲突？
  - 产出 PASS 或 FLAG（附简要说明）
- **特点**: 轻量级、高频、低延迟
- **模型**: `deepseek-v4-flash` (non-thinking 模式)，温度 0.3。简单检查任务，高频调用，成本优先
- **调用频率**: 每章 N+1 次（N=场景数）

#### 3B — 质量审阅者 (Quality Reviewer)
- **写后介入**: 整章完成后深度审阅
- **检查维度**: 节奏是否合理、情节逻辑是否有漏洞、文笔质量、是否偏离大纲
- **产出**: 审阅报告 → 合格 / 需修改 / 需重写，附具体修改建议
- **模型**: `deepseek-v4-flash` (thinking 模式)，温度 0.3。需要分析推理但每章仅1次
- **调用频率**: 每章1次

---

### 模块四：状态管理师 (State Manager)

职责：维护全局状态表，保证跨章节一致性，是整个框架长文本记忆的核心。

#### 4A — 状态更新师 (State Updater)
- **输入**: 新完成章节全文 + 上一版本所有状态表
- **输出**: 更新后的状态表（diff + 新版本完整数据）
- **状态表结构**:
  1. **角色状态表**: 每个角色的当前状态（位置、等级/能力、情绪、关系变化、生死）
  2. **伏笔登记表**: 已埋伏笔、预计回收章节、当前状态（未回收/部分回收/已回收）
  3. **世界状态表**: 世界层面变化（某国灭亡、某地异变等）
  4. **未解决冲突表**: 当前活跃冲突线及其进展
- **模型**: `deepseek-v4-flash` (non-thinking 模式)，温度 0.2。结构化信息提取，任务直白但需精确
- **调用频率**: 每章1次

#### 4B — 简报生成师 (Brief Generator)
- **输入**: 更新后的状态表 + 下一章章大纲
- **产出**: 下一章写作简报（启动下一轮作者模块的核心上下文包）
- **简报内容**:
  - 下一章需出场的角色及其当前状态
  - 需要推进的伏笔
  - 上一章结尾的衔接点
  - 监督者特别提醒的注意事项
  - 相关世界观设定片段（按需检索）
- **模型**: `deepseek-v4-flash` (thinking 模式)，温度 0.3。需要综合多个状态表做推理
- **调用频率**: 每章1次

---

## 三、章节级工作流

```
一章的完整创作循环：

 1. 编排器拿到"下一章大纲"
 2. 状态管理师(4B) 生成"下一章写作简报"
 3. 作者(2A) 产出"场景级大纲" ────→ 监督者(3A) 审核 ──→ 通过/修改
 4. 作者(2B) 写 场景1 ──→ 监督者(3A) 轻量检查 ──→ 继续/返工
 5. 作者(2B) 写 场景2 ──→ 监督者(3A) 轻量检查 ──→ 继续/返工
    ... (重复直到所有场景完成)
 6. 作者(2C) 拼接润色 → 完整章节
 7. 监督者(3B) 深度审阅 ──→ 通过/修订/重写
 8. 状态管理师(4A) 更新所有状态表
 9. 状态管理师(4B) 生成下一章简报
10. 返回步骤 1，开始下一章
```

**人工介入点**: 以上任意步骤都可以人工查看和干预。默认全自动运行。

---

## 四、模型配置总结

### DeepSeek V4 系列说明

旧模型 `deepseek-chat`(V3) 和 `deepseek-reasoner`(R1) 已被统一为 `deepseek-v4-flash`，通过模式切换实现两类能力：

| 旧名称 | 新模式 | 说明 |
|--------|--------|------|
| `deepseek-chat` (V3) | `deepseek-v4-flash` non-thinking | 通用生成，速度快 |
| `deepseek-reasoner` (R1) | `deepseek-v4-flash` thinking | 推理增强，CoT 输出 |

同时新增 **`deepseek-v4-pro`**，是更强大的高端模型（约 Flash 3x 价格），同样支持 thinking/non-thinking 双模式。当前享受 75% 折扣（至 2026/05/31）。

### Agent 模型分配

| 梯队 | Agent | 模型 | 模式 | 温度 | 频率 | 选择理由 |
|------|-------|------|------|------|------|----------|
| **输出质量** | 2B 场景写手 | **v4-pro** | non-thinking | 0.8-1.0 | 每章3-8次 | 读者直接看到的内容，质量决定成败 |
| **输出质量** | 2C 章节润色师 | **v4-pro** | non-thinking | 0.6 | 每章1次 | 面向读者的最终文本 |
| **低频深度** | 1A 世界观构建师 | **v4-pro** | thinking | - | 全书1次 | 复杂设计，调用少，成本可忽略 |
| **低频深度** | 1B 情节设计师 | **v4-pro** | thinking | - | 每卷/弧 | 复杂结构推理，调用少 |
| **中频推理** | 2A 章节规划师 | v4-flash | thinking | 0.5 | 每章1次 | 需结构化分析 |
| **中频推理** | 3B 质量审阅者 | v4-flash | thinking | 0.3 | 每章1次 | 需分析但性价比重要 |
| **中频推理** | 4B 简报生成师 | v4-flash | thinking | 0.3 | 每章1次 | 综合多份状态表做推理 |
| **中频精确** | 4A 状态更新师 | v4-flash | non-thinking | 0.2 | 每章1次 | 结构化提取，任务直白 |
| **高频轻量** | 3A 一致性守护者 | v4-flash | non-thinking | 0.3 | 每章N+1次 | 简单检查，高频调用，成本第一 |

### 选型逻辑

- **输出质量线**（2B、2C）用 Pro：这两个 Agent 的产出就是最终小说文本，多花的 3x 成本换来更好的文笔和连贯性，值
- **复杂设计线**（1A、1B）用 Pro：全书只调用几次，总成本微不足道，但世界观和大纲的质量影响全书
- **分析推理线**（2A、3B、4B）用 Flash thinking：需要 CoT 推理但不值得 Pro 溢价
- **高频底线**（3A）用 Flash non-thinking：每章可能调用 5-10 次，积少成多，且检查任务不需要深层推理
- **精确提取线**（4A）用 Flash non-thinking：更新状态表是结构化的信息抽取任务，不需要推理链

---

## 五、存储方案

| 存储层 | 技术 | 存储内容 | 理由 |
|--------|------|----------|------|
| 结构化状态 | SQLite | 角色状态表、伏笔登记表、世界状态表、章节元数据表 | 支持精确查询和更新 |
| 文档/文本 | Markdown 文件 | 完整章节文本、大纲文档、世界设定文档、角色档案 | 方便人工查看和修改 |
| 语义检索 | ChromaDB | 所有章节/设定/角色的 embedding | 解决跨章节细节检索 |
| 配置 | JSON/YAML | Agent prompt 模板、模型参数、工作流配置 | 方便调整和版本管理 |

---

## 六、人工介入机制

### 简易版（首版实现）

**核心机制**: 文件级介入 + 拦截器模式

1. 所有 Agent 的输出都保存为 Markdown 文件
2. 人工可以随时打开文件修改，修改后保存为 `<原名>_edited.md`
3. 系统读取时优先检查是否存在 `_edited` 版本
4. 关键节点（弧大纲、章大纲）通过配置文件可设为"需审批"模式

```python
# 拦截器伪代码
class HumanInterceptor:
    checkpoint_policy = {
        "architect.1b.arc_outline":   "require_approval",
        "architect.1b.chapter_outline": "auto_pass",
        "supervisor.3b.quality_review": "notify",
        "author.2a.scene_plan":       "auto_pass",
    }
    
    def intercept(self, agent_name, output):
        policy = self.checkpoint_policy.get(agent_name, "auto_pass")
        if policy == "auto_pass":
            return output  # 记录日志后放行
        elif policy == "notify":
            save(output)   # 保存通知队列
            return output  # 不阻塞
        elif policy == "require_approval":
            save(output)
            return wait_for_human_or_timeout(output)
```

### 复杂版（后续扩展）

- Web UI 实时预览写作进度
- 内联编辑界面
- 分支创作（同大纲 A/B 版本对比选择）
- 角色对话模拟器

---

## 七、长文本记忆策略

### 核心依赖
状态管理师维护的**四张状态表**是保证跨章节一致性的核心：
- 角色状态表
- 伏笔登记表
- 世界状态表
- 未解决冲突表

### 章节规划师的智能检索

章节规划师 (2A) 根据当前章节性质决定检索策略：

| 章节类型 | 检索重点 | 内容 |
|----------|----------|------|
| **延续型** | 情节连贯 + 风格一致 | 前3章完整文本、出场角色台词片段、角色状态表(节选)、全局铁律(精简版) |
| **新展开型** | 设定完整 + 伏笔推进 | 前1章结尾段落、角色状态表(完整版)、相关世界观设定、伏笔登记表(未回收项) |

编排器通过计算当前大纲与前几章的语义相似度来辅助判断章节类型，而非硬编码两种模式。

---

## 八、实施中新增的关键设计

### 8.1 事实一致性防御系统

**问题**：上游规划层（1B 卷大纲、2A 章节规划）会从 plot_structure 凭空编造细节（口哨、摇篮曲、密钥分段等），这些虚构内容经写手忠实执行后与已确立的事实矛盾。

**解决**：六层防御

| 层 | 位置 | 机制 |
|----|------|------|
| 1 | 3B 审阅 | 每章完成后生成 `fact_digest`（确定/未出现/悬念） |
| 2 | 1B 卷大纲 | 增量更新：锁定已完成部分，新增细节必须从 fact_digest 提取，3A 审核后保存 |
| 3 | 2A 规划 | 输入含 fact_digest + 禁止清单，"如与事实摘要冲突以摘要为准" |
| 4 | 2B 写手 | fact_digest 作为最高优先级上下文，编造行为特征直接判定不合格 |
| 5 | 上下文 | 写手接收10项上下文，fact_digest 置顶 |
| 6 | 存储 | canonical 文件 + .bak 回退，任何步骤可安全回滚 |

### 8.2 Canonical 文件系统

- 设定/大纲/规划使用固定文件名（无时间戳），每次覆盖时自动 `.bak` 备份
- `_edited.md` 优先读取，人工介入不受影响
- 章节正文/简报/审阅保留时间戳存档
- 架构级 `snapshot_all()` / `rollback_all()` 快照回退

### 8.3 写手上下文重构

写手输入优先级（从高到低）：
1. 前文章节事实摘要（防止凭空引用）
2. 世界观设定
3. 卷大纲背景
4. 角色档案
5. ChromaDB 原文验证片段
6. ChromaDB 设定检索片段
7. 上一章审阅报告
8. 角色与世界状态
9. 已完成场景正文（必读，前文锁定为永久规则）
10. 上一场景结尾（强制衔接，第一句话必须从此处直接写）

### 8.4 章节规划增强

- 已完成场景保留完整描述 + 三个实际字段（引入设定/新埋伏笔/回收伏笔）
- 追踪节：累计汇总表，标注首次出现场景和预计回收方式
- 每场景后强制 auto-replan，重新解析场景列表
- 5场景硬上限，更快触发卷大纲更新

### 8.5 回退与独立接口

- `rollback_chapter(N)` — 删除指定章的全部场景/规划/简报
- `build_chapter_plan(N)` — 已完成场景→重建章节规划（含设定伏笔追踪）
- `push_chapter_to_storage(N)` — 章节全文→设定集/大纲/SQLite/ChromaDB

### 8.6 数据存储约定

| 数据类型 | 存储方式 | 生命周期 |
|---------|---------|---------|
| 设定变更 | world_setting.md (canonical, 智能合并) | 跨章持久 |
| 角色状态 | SQLite + world_setting.md 角色档案节 | 跨章持久 |
| 伏笔 | SQLite (pending→resolved) | 跨章持久 |
| 章节事实 | fact_digest_chNNNN.md (canonical) | 跨章持久 |
| META | plot_structure.md 章节标记 | 跨章持久 |
| 章节规划 | scene_plan_chNNNN.md (canonical) | 单章 |
| 简报/审阅 | briefs/*.md (时间戳) | 单章，人工审核 |

---

## 九、待定事项

1. 人工高度参与 vs AI 全自动的网文质量对比实验 —— 需先完成简易版后再设计
2. 卷大纲和弧大纲的具体格式规范 —— 首版实现时细化
3. 风格指南的具体定义（模仿特定作者、特定流派风格等）—— 首版实现时细化
4. 规划层编造问题仍需持续优化 —— fact_digest + 禁止清单能大幅减少但不能完全消除，关键场景可能需要手写 spec

---

> 最后更新: 2026-05-22
> 状态: 核心系统已实现，nuclear_cultivation 第1章场景1-2创作中
